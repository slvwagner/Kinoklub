---
title: "Site-Map"
output: 
  html_document
---

```{r setup, include=FALSE}
library(lubridate)
knitr::opts_chunk$set(echo = FALSE)
act_date <- Sys.time()|>as.Date()
act_date <- paste0(day(act_date),".", month(act_date),".", year(act_date))
```

Site Map wurde am `r act_date` erstellt.

```{css}
body {
  background-color: #0D262A;
  color: #f4cccc;
}

a {
  color: #f4ccfc; /* Set your desired color using a hex code, RGB, or color name */
  text-decoration: none; /* Optional: Remove the default underline */
}

a:hover {
  color: #C623D1; /* Set a different color when the link is hovered over (optional) */
}


```


## Statistik

## Archiv

## Abrechnung Filmvorführungen

## Verleiherabrechnung

## Jahresrechnung
